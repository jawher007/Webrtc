<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <style>
    

    #photo {
  border: 1px solid black;
  box-shadow: 2px 2px 3px black;
  width:320px;
  height:240px;
}
#canvas {
  display:none;
}

.output {
  width: 340px;
  display:inline-block;
}
.flexing{
  display: flex;
}
#startbutton,#start,#filters {
 
  bottom:32px;
  background-color: rgba(0, 150, 0, 0.5);
  border: 1px solid rgba(255, 255, 255, 0.7);
  box-shadow: 0px 0px 1px 2px rgba(0, 0, 0, 0.2);
  font-size: 14px;
  font-family: "Lucida Grande", "Arial", sans-serif;
  color: rgba(255, 255, 255, 1.0);
}
  </style>
  <script>
    const ROOM_ID = "<%= roomId %>"
    localStorage.setItem("userid",ROOM_ID+"");
  </script>
<script  src="https://unpkg.com/peerjs@1.3.1/dist/peerjs.min.js" defer></script>
  
</script>
  <script src="/socket.io/socket.io.js" defer></script>
  <script src="script.js" defer></script>
  <title>Document</title>
  <style>
    #video-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, 300px);
      grid-auto-rows: 300px;
      column-gap: 1em;
    }
    
    video {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
  </style>
</head>
<body>
  <div class="flexing"><button id="start" onclick="Start()">
    Start
  </button>
  <button id="startbutton">
    Take a picture
  </button>
  <select id="filters" onchange="myFunction(value)">
    <option value="None">none</option>
    <option value="contrast(175%)">Contrast</option>
    <option value="sepia(100%)">Sepia</option>
    <option value="saturate(200%)">saturate</option>
    <option value="opacity(50%)">opacity</option>
    <option value="invert(100%)">invert</option>
    <option value="hue-rotate(90deg))">hue-rotate</option>
    
    </select>
  </div>

<br>

  <div id="video-grid"></div>
  <canvas id="canvas">
  </canvas>
  <br>
  <div class="output flexing">
    <img id="photo" alt="The screen capture will appear in this box.">
  </div>
</body>
</html>